---
const { title } = Astro.props;
---

<html>
  <body class="bg-gray-900 text-gray-300 py-10 md:py-16">
    <main class="max-w-3xl mx-auto px-6 lg:px-0">
      <h1
        class="text-5xl md:text-6xl font-semibold text-white text-center mb-8"
      >
        {title}
      </h1>

      <article>
        <slot />
      </article>
    </main>
  </body>
</html>

<style is:global>
  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );
  }

  html {
    font-family: ui-serif, Georgia, Cambria, "Times New Roman", Times, serif;
    background: #13151a; /* Your 'bg-gray-900' */
    background-size: 224px;
    /* Helps browser with dark mode defaults */
    color-scheme: dark;
    /* Smooths font rendering */
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  /* --- STYLING YOUR MARKDOWN CONTENT --- */

  /* This is the "Owl Selector". 
    It adds a top margin to all elements inside 'article'
    *except* the first one. This creates all the vertical
    spacing for your paragraphs, lists, and headers automatically.
  */
  article > * + * {
    margin-top: 1.25em;
  }

  h2 {
    font-size: 2em;
    font-weight: 600;
    color: white;
    margin-top: 2em; /* More space *before* a new section */
    margin-bottom: 0.75em; /* Less space *after* it */
  }

  h3 {
    font-size: 1.5em;
    font-weight: 600;
    color: white;
    margin-top: 2em;
    margin-bottom: 0.5em;
  }

  p {
    /* 18px is a perfect, readable size for blog text */
    font-size: 1.125rem;
    /* This is the #1 fix for readability. 
       Taller lines are easier to read. */
    line-height: 1.75;
  }

  /* Style for links */
  a {
    color: rgb(var(--accent-light));
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 3px;
  }

  /* Style for inline code */
  code {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      Liberation Mono,
      DejaVu Sans Mono,
      Bitstream Vera Sans Mono,
      Courier New,
      monospace;
    background: #2d333b; /* A slightly lighter dark */
    padding: 3px 6px;
    border-radius: 4px;
    font-size: 0.9em;
    /* Prevents code from breaking mid-word */
    word-wrap: break-word;
  }

  /* Add any other elements you need... */
  ul,
  ol {
    margin-left: 1.5rem;
    list-style: revert; /* Use browser default bullets */
  }

  blockquote {
    border-left: 4px solid rgb(var(--accent-dark));
    padding-left: 1rem;
    font-style: italic;
    color: #9ca3af; /* text-gray-400 */
  }
</style>
